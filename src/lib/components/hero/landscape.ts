// hero/landscape.ts
// Extracted from genGhibliPage — landscape banner hero.
// Simple full-width CSS-only landscape illustration banner.
// The about/polaroid section that follows is NOT part of this hero.
import type { SectionContext, SectionVariantFn } from "../types";

export const heroLandscape: SectionVariantFn = (_ctx) => {
  return `
      {/* ===== GHIBLI LANDSCAPE BANNER ===== */}
      <div className="ghibli-landscape" />`;
};
